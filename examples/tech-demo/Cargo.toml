[package]
name = "tech-demo"
version = "0.1.0"
edition = "2024"

[features]
tracy = ["game-engine/tracy"]

[dependencies]
game-engine = { path = "../.." }
glam = { version = "0.30.1" }
cfg-if = "1.0.0"
env_logger = "0.11.8"
