pub mod mtl_loader;
pub mod obj_loader;
pub mod plugin;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
