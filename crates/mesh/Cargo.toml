[package]
name = "mesh"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
essential = { path = "../essential" }
bytemuck = { version = "1.22.0", features = ["derive"] }
glam = { version = "0.30.1" }
ecs = { path = "../ecs" }