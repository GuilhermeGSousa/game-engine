[package]
name = "color"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
bytemuck = { version = "1.22.0", features = ["derive"] }
encase = { version = "0.11.1" }
rand = { version = "0.10.2" }
serde = { version = "1", features = ["derive"] }

# rand seeds its RNG from OS entropy via getrandom, which has no default
# backend on wasm32-unknown-unknown; opt in to the browser's
# crypto.getRandomValues through the JS backend.
[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.4", features = ["wasm_js"] }
